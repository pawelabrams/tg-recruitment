nelmio_api_doc:
    documentation:
        info:
            title: Notification Publisher
            description: TG recruitment challenge
            version: 0.1.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
